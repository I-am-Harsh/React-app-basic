import { PROMOTIONS } from "../shared/promotions";
// import { PROMOTIONS } from './../shared/promotions';

export const Promo = (state = PROMOTIONS, action ) => {
    switch(action.type) {
        default:
            return state;
    }
}